

var objList = {};
var bgSrc = "../imgs/bg.jpg";
var layerSrc = "../imgs/layer.png";

var main = function () {
    
    var game = new EyeSpy();
        game.SetGame(objList, bgSrc, layerSrc);
    
   // document.body.appendChild(game.element); uncomment once element is a object. 
}