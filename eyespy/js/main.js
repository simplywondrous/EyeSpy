

var gameObject = {};

var main = function () {
    
    var game = new EyeSpy();
        game.SetGame(gameObject);
    
   // document.body.appendChild(game.element); uncomment once element is a object. 
}